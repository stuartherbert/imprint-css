{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "../tsconfig.build.json",
    "display": "Recommended",
    "compilerOptions": {
        "rootDir": "src",
        "outDir": "./lib",                        /* Redirect output structure to the directory. */
        "baseUrl": ".",
        "paths": {
            "@imprintcss/tailwind-plugin-types": [ "../tailwind-plugin-types" ],
        }
    },
    "include": [
        "src"
    ]
}