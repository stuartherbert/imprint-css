---

interface Props {
    [name: string]: string;
}

const { ...attributes } = Astro.props;
attributes.class = "imprint-callout --imprint-primary --standout " + attributes.class;

---

<div {...attributes}>
    <slot/>
</div>